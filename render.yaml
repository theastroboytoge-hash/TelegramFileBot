services:
  - type: web
    name: dilemmapl-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: PORT
        value: 10000
      - key: TOKEN
        sync: false
      - key: ADMIN_ID
        sync: false
